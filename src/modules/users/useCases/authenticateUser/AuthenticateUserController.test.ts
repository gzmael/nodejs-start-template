describe('Authenticate User Controller', () => {
  it('Should be able authenticate a user', async () => {
    expect({
      message: 'Hello World',
    }).toEqual({
      message: 'Hello World',
    });
  });
});
